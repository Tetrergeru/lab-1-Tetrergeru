name: "Deploy"
on:
  push:
    branches:
      - master
    tags:
    - '*'
jobs:
  deploy-to-githubio:
    runs-on: ubuntu-latest
    steps:
    - name: Deploy
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./public